# Personal env backup (private values not suitable for public repo):
# https://www.notion.so/Dotfile-3072af89649280588957d18e20d059cf

# --- Git Configuration ---
# GIT_USER_NAME="Eric Yeh"
# GIT_USER_EMAIL="eric91343@gmail.com"

# --- Git Workspaces Configuration ---
# Define a list of workspaces (space-separated) (only a-z, A-Z, 0-9)
# GIT_WORKSPACES="company1 company2"

# For each workspace, define variables if need:
# GIT_WORKSPACE_<NAME>_EMAIL
# GIT_WORKSPACE_<NAME>_NUGET_URL

# Example
# GIT_WORKSPACE_company1_EMAIL="user@company1.com"
# GIT_WORKSPACE_company1_NUGET_URL="https://nuget.company1.com/v3/index.json"
# GIT_WORKSPACE_company2_EMAIL="user@company2.com"

# --- AI Commit Configuration ---
# OpenAI API Key (required for openai service)
# OPENAI_API_KEY=your_openai_api_key_here

# LLM service priority order (comma-separated)
# Options: claude, gemini, copilot, openai
# Priority: Env Var > Git Config (dotfiles.ai.llm-priority) > Default
# Default: claude,ccr,gemini,copilot,openai
# GIT_COMMIT_LLM_PRIORITY="claude,ccr,gemini,copilot,openai"
